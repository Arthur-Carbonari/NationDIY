import { IsOwnerDirective } from './is-owner.directive';

describe('IsOwnerDirective', () => {
  it('should create an instance', () => {
    const directive = new IsOwnerDirective();
    expect(directive).toBeTruthy();
  });
});
